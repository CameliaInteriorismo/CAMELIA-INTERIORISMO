/**
 * The form's one input treatment — hairline underline-less box on the cream
 * ground, darkening its border on focus. Shared so the address combobox and
 * the plain inputs beside it can't drift apart.
 */
export const fieldClass =
  "border-primary/30 text-primary placeholder:text-primary/35 focus:border-primary w-full border bg-transparent px-4 text-sm outline-none transition-colors duration-300";
